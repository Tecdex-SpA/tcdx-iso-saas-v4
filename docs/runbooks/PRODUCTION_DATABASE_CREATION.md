# Production Database Creation

Status: `DB_N05_READY_FOR_REVIEW`. Local gates only; production creation remains prohibited until integral Human Review DB-N01 -> DB-N05.

Do not execute real creation of tcdx_saasv2 until integral Human Review DB-N01 -> DB-N05 and a separately authorized infrastructure/secret/rollback plan. The immutable QA export is already materialized; do not rerun it.

## Scope

Create a new PostgreSQL production database from a clean baseline. Do not copy the QA database wholesale.

## Inputs

- Approved infrastructure target.
- Approved PostgreSQL major version and extension availability.
- Secret-managed passwords outside this repository.
- Approved customer data migration list, if any.
- DB-N05 artifacts:
  - `database/baseline/production_schema_v1.sql`
  - `database/baseline/production_seed_v1.sql`
  - `scripts/normalization/db-n05-readonly-qa-preflight.sql`
  - `artifacts/db-n05/PRODUCTION_DATA_MIGRATION_MATRIX.md`
  - `artifacts/db-n05/RUNTIME_TO_BASELINE_COVERAGE.md`
  - `artifacts/db-n05/REFERENCE_CATALOG_COVERAGE.md`
  - `artifacts/db-n05/PRODUCTION_ROLE_PRIVILEGE_MATRIX.md`
  - `artifacts/db-n05/DBN01_N04_BASELINE_PARITY.md`

## Latest local evidence

PostgreSQL 16.15 with the local pgvector:pg16 image now passes the isolated fresh/rerun/functional/multitenant/integrity/Health/startup gates. Effective SET ROLE checks, explicit function execute allowlists and 15-table regulatory parity pass. The versioned reference gate now passes for three published versions and transition-only FDIS; this is review readiness, not authorization to create production.

The helper uses localhost-only publishing, tmpfs and automatic container/volume removal. Existing local initdb mode remains available but requires pgvector installed for that PostgreSQL major version. The real loader's two runs preserve catalog identities/content; KB derived-child surrogate IDs are regenerated by its existing rebuild semantics.

## Current Blocking Gates

Do not create a real production database from DB-N05 while any of these remain false:

- `scripts/normalization/db-n05-runtime-schema-contract.test.js` must report `active_missing=0`.
- `artifacts/db-n05/REFERENCE_CATALOG_COVERAGE.md` must report production reference catalogs reproducible and complete.
- `artifacts/db-n05/PRODUCTION_ROLE_PRIVILEGE_MATRIX.md` must keep effective privilege PASS, including no schema-wide `EXECUTE ON ALL FUNCTIONS`.
- `ai_reader` must either have no direct read grant or a tenant-safe read path with isolation tests.

## Creation Order

1. Provision PostgreSQL with storage, backups, monitoring, TLS, and network allowlist.
2. Create login roles and passwords through the secret manager, not SQL files:
   - migration/admin role
   - backend runtime role
   - platform runtime role
   - AI reader role
   - read-only support/audit role if approved
3. Confirm required extensions:
   - `pgcrypto`
   - `pg_trgm`
   - `vector` REQUIRED; extension, vector column and cosine-distance operator must pass in the isolated fresh harness first
4. Create empty production database.
5. Provision NOLOGIN role identities and required extensions as infrastructure owner; give migration admin CREATE on the empty database/public schema, then apply `database/baseline/production_schema_v1.sql` under that role. This separation is covered by the effective bootstrap test.
6. Apply `database/baseline/production_seed_v1.sql` as migration/admin.
7. Run the existing `scripts/normalization/load-production-reference-catalogs.js` using the approved migration connection (mandatory for commercial/math/KB catalogs), then verify baseline:
   - no `qa_audit` schema
   - no backup/cleanup/preview tables
   - DB-N01 same-control identity constraints present
   - DB-N02 formula/run/output/snapshot authority present
   - DB-N03 composite FK constraints present
   - DB-N04 tenant context functions/readiness view present
   - runtime-to-baseline coverage `active_missing=0`
   - production reference catalogs complete
   - role privilege matrix accepted
   - function execute grants are explicit allowlists; future app-owned functions have no default public execute
   - `ai_reader` safe defer or tenant isolation test PASS
8. Load approved real customer data only if approved:
   - run read-only QA preflight first
   - classify rows with `PRODUCTION_DATA_MIGRATION_MATRIX.md`
   - migrate tenant by tenant
   - validate composite FKs and duplicate natural keys after each tenant
9. Configure backend environment to point to the new database.
10. Run smoke tests:
    - `/live`
    - `/ready`
    - tenant login
    - RBAC entitlement
    - tenant standard activation
    - tenant controls
    - evidence/finding/action creation
    - GRC Health read
11. Take post-create backup.
12. Record go-live decision and rollback deadline.

## RLS Posture

DB-N05 does not enable broad RLS. Production posture remains:

`RLS_DEFERRED_UNTIL_AI_READER_READY`

Until then, production protection relies on:

- backend tenant scope enforcement
- DB-N04 transaction-local tenant context
- DB-N03 composite same-tenant FKs
- explicit platform transaction path
- no BYPASSRLS grant to tenant runtime role

Enable broad RLS only after AI Engine/`ai_reader` tenant context or tenant-safe views are proven. Until then, do not grant `ai_reader` direct `SELECT` on tenant-bearing schemas/views.

## Backup And Rollback

Before go-live:

1. Create pre-go-live snapshot.
2. Run `pg_dump --format=custom` for the fresh database after baseline/seed and before data migration.
3. If customer data migration is performed, dump again after each approved tenant batch.
4. Restore the dump into an isolated database and run baseline smoke checks.
5. Define rollback decision point before switching backend traffic.

Rollback choices:

- Before traffic switch: discard fresh database and keep current environment.
- After traffic switch but before writes: switch backend config back and keep fresh DB for forensic review.
- After traffic and writes: use documented restore point and reconcile writes explicitly; do not manually patch production rows ad hoc.

## Not Executed By DB-N05

- No production database was created.
- No migration was applied to `db-v4`.
- No QA dump was copied.
- No commit, push, merge, or deploy was performed.

## Versioned ISO reference contract

| Version | Controls | Evidence expectations | Publication | Certifiable | Status |
| --- | ---: | ---: | --- | --- | --- |
| ISO9001:2015 | 16 | 13 | published | true | approved_for_loader |
| ISO27001:2022 | 21 | 19 | published | true | approved_for_loader |
| ISO42001:2023 | 16 | 14 | published | true | approved_for_loader |
| ISO9001:2026_FDIS | 8 | 8 | transition_prep | false | transition_only |

The reviewed authority is `iso_controls` plus same-version `iso_evidence_expectations` from this export. Coverage means faithful materialization of the approved product reference snapshot, whose source_policy is copyright_safe_summary; it does not assert reproduction of the full normative publications.

ISO9001:2026_FDIS is transition preparation only. It has no `product_standard_code`, no row in the selectable `standards` compatibility catalog and no projection into `controls_catalog`. It does not replace ISO9001:2015 and must not be sold or represented as final/certifiable ISO9001:2026. Absence of a final 2026 catalog is not a blocker for this review gate. Historical ISO27701/27017/27018 compatibility is preserved and outside current blockers.

The sole orchestrator remains `scripts/normalization/load-production-reference-catalogs.js`. Manifest v2 stores raw standard_code/version_code, explicit product compatibility code, publication/certification status, loader_status, controls/evidence/metadata paths, exact counts and SHA-256 for each file. Three entries are approved_for_loader; FDIS is transition_only. Every file and same-version evidence mapping is validated before any reference write, and before the main orchestrator connects. Pending/unapproved states, checksum or count mismatches, missing titles, duplicate controls/evidence and cross-version references fail closed.

Fresh baseline adds `iso_standards`, `iso_standard_versions`, `iso_controls`, `iso_evidence_expectations`, with natural-key uniqueness and composite same-version FKs. New surrogate IDs come from PostgreSQL, not QA. Runtime backend/platform/support have SELECT only on these four tables; ai_reader has no access; migration admin loads references. No schema-wide grants or new functions were added.

All four sources load into versioned tables. Only the three approved published versions project to existing product standards/controls/mappings. Existing 30 KB-derived ISO27001 codes and their compatibility behavior remain; metadata marks them compatibility_only. They are not counted as approved versioned controls and no semantic equivalence to QA ISMS codes is invented. Fresh controls_catalog/mappings=83: 53 approved projected controls + 30 preserved KB codes. QA controls_catalog's 118 exported rows and 118 mappings are not imported.
