# Fase 5 — BI Dashboard Builder

Implementado:

- `dashboard_definitions`
- `dashboard_widgets`
- `dashboard_permissions`
- publicación;
- snapshot;
- render con widgets y advertencias.

Widgets soportados:

`kpi_card`, `trend`, `heat_map`, `distribution`, `matrix`, `table`, `timeline`, `status`, `gauge`, `relationship_graph`, `impact_graph`.

La UI usa layout persistido por filas/columnas; no se agregó drag-and-drop.
