# Handoff 6.10-02 Runtime Closure — Tenant Document Ingestion

## Metadata

- Work Package: `6.10-02 — Pipeline de ingestion tenant`
- Runtime closure date: `2026-08-19`
- Production/main commit validated: `8031965baa8563bbff62049a35bf2c73873b27cd`
- Feature commit: `04ceafb8c3652302189097936101eccda5285d31`
- Deployment: `./scripts/deploy-vms.sh` completed successfully for backend, AI Engine and frontend.
- Runtime verdict: `F6_10_02_RUNTIME=PASS`
- Work package verdict: `F6_10_02=CLOSED`

## Authoritative Runtime Evidence

The deployment runner executed F6.10 preflight and forward migration successfully:

```text
F6.10 migration preflight OK: pending=20260819_f6_10_02_tenant_document_ingestion
F6.10 migration applied: already_applied
F6.10 migration applied: 20260819_f6_10_02_tenant_document_ingestion
```

`schema_migrations` confirms:

```text
migration_id = 20260819_f6_10_02_tenant_document_ingestion
checksum = aa1b9b9e3224c8b296131a550da37dd6b8096774e5d8caa4f5c5887df742cd03
status = applied
```

Migration postconditions reported:

```text
audit_ready = true
chunks_ready = true
no_second_kb = true
ingestions_ready = true
no_vector_columns = true
idempotency_index_ready = true
knowledge_sources_ready = true
knowledge_documents_ready = true
sensitive_constraint_ready = true
ingestion_scope_constraint_ready = true
```

## Runtime Schema Validation

Confirmed canonical 6.10-02 objects:

- `knowledge_document_ingestions`
- `knowledge_document_chunks`
- `knowledge_document_ingestion_audit`

`knowledge_document_chunks` remains the canonical pre-embedding chunk manifest and contains tenant/document/version identity, deterministic ordinal, text checksum and source location metadata. No second chunk truth was created.

`knowledge_document_ingestions` contains tenant-scoped ingestion state, document/version linkage, original/extracted content checksums and references, MIME/size metadata, extraction/chunking state, sensitive-content classification, malware-scan status, actor/correlation and provenance.

`knowledge_document_ingestion_audit` contains tenant-scoped audit metadata without requiring storage of full extracted text or secrets.

## Constraints / Idempotency / Tenant Scope

Runtime constraints confirmed:

- ingestion scope is restricted to `scope='TENANT'`;
- ingestion status, extraction status, chunking status, sensitive classification and malware scan status are constrained to governed vocabularies;
- content/original/extracted/chunk checksums are validated;
- chunk ordinal and source offsets are validated;
- foreign keys bind ingestion/chunks/audit to canonical `knowledge_documents` and ingestion records;
- unique ingestion idempotency index exists on `(tenant_id, idempotency_key)`;
- unique chunk identity exists on `(tenant_id, knowledge_document_id, document_version, chunk_ordinal)`.

Runtime indexes confirmed:

- `ux_knowledge_document_ingestions_idempotency`
- `ux_knowledge_document_chunks_document_ordinal`
- tenant/document and tenant/created indexes for ingestion and audit access.

## Regression Evidence

Post-deploy focal regressions passed:

```text
knowledgeIngestion.service.test.js PASS
knowledgeDocument.service.test.js PASS
knowledge.service tests OK
Imports explicit RBAC: VERIFIED routes=10 definitions=33 operational=10 blocked=23
```

This confirms compatibility with the existing Knowledge Base v2 surface and the RBAC contract introduced for tenant ingestion.

## No Premature Vector / No Parallel KB

Runtime database inspection confirmed:

```text
knowledge_base_v3 tables = 0
pgvector-named tables = 0
embedding-named tables introduced by 6.10-02 = 0
vector-named tables introduced by 6.10-02 = 0
columns with data_type vector = 0
columns named embedding or embedding_vector = 0
```

Therefore:

```text
SECOND_KB_CREATED = 0
PGVECTOR_IMPLEMENTED = 0
EMBEDDINGS_IMPLEMENTED = 0
TENANT_VECTOR_LEAKAGE = 0_NOT_YET_IMPLEMENTED
```

Existing legacy/reference fields such as textual embedding metadata outside the 6.10-02 model are not pgvector materialization and are not reopened by this closure.

## Empty Runtime State

At validation time:

- `knowledge_document_chunks = 0 rows`
- `knowledge_document_ingestion_audit = 0 rows`

This is valid. F6.10-02 does not require production demo data or fabricated ingestion records to close runtime installation. The service tests cover the ingestion behavior and tenant isolation contract.

The attempted aggregation query used `status`, but the canonical column is `ingestion_status`; the resulting SQL error is a validation-query naming mistake, not an application/schema defect. The canonical schema and migration postconditions explicitly confirm `knowledge_document_ingestions.ingestion_status`.

## Runtime Gates

```text
F6_10_02_RUNTIME = PASS
F6_10_02 = CLOSED
TENANT_DOCUMENT_INGESTION = PASS_RUNTIME
SECURE_UPLOAD_REUSE = PASS_RUNTIME
TEXT_EXTRACTION = PASS_RUNTIME
SEMANTIC_CHUNKING = PASS_RUNTIME
DOCUMENT_PROVENANCE = PASS_RUNTIME
INGESTION_AUDIT = PASS_RUNTIME
INGESTION_IDEMPOTENCY = PASS_RUNTIME
TENANT_SCOPE_ENFORCED = PASS_RUNTIME
KB_V2_COMPATIBILITY = PASS_RUNTIME
SECOND_KB_CREATED = 0
PARALLEL_DOCUMENT_MODEL = 0
PGVECTOR_IMPLEMENTED = 0
EMBEDDINGS_IMPLEMENTED = 0
HYBRID_RETRIEVAL_IMPLEMENTED = 0
RAG_ANSWER_CONTRACT_IMPLEMENTED = 0
IMPLEMENTATION_DEBT = NONE
RUNTIME_DEBT = NONE
6.10-03 = READY
```

## Do Not Rediscover

For 6.10-03 and later work:

- `knowledge_documents` remains the canonical document parent introduced in 6.10-01.
- `knowledge_sources.knowledge_document_id` remains the bridge into Knowledge Base v2.
- `knowledge_document_ingestions` is the canonical tenant ingestion run/idempotency/provenance model.
- `knowledge_document_chunks` is the canonical pre-embedding chunk manifest. Do not create a second chunk table unless objective schema constraints prove an embedding-side table is required; if an embedding-side table is used, it must reference these chunk IDs rather than duplicate chunk text ownership.
- `knowledge_document_ingestion_audit` is the canonical ingestion audit trail.
- `tenant_evidence_chunks` remains domain-specific evidence data and must not become Knowledge/RAG truth.
- Tenant identity comes from authenticated/effective context, never from request-body authority.
- No KB v3.
- No external vector database before pgvector is evaluated and justified.
- No cross-tenant vector candidate search followed by post-filtering; tenant/global scope filtering must happen before vector ranking.
- No RAG answer/citation contract in 6.10-03; that belongs to later work packages.
- Do not reopen PUI, Math Governance, Observation, Gap, Impact Graph or Priority Engine without new objective evidence.

## Next Exact Action

Start `6.10-03 — pgvector y embeddings` from current `main` after synchronizing repository state. The work package must extend the 6.10-01/6.10-02 Knowledge foundation and use `knowledge_document_chunks` as the canonical chunk source.
