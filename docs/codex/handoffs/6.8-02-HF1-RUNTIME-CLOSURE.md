# F6.8-02-HF1 — Runtime Closure

Date: 2026-08-18
Repository: `Tecdex-SpA/tcdx-iso-saas-v4`
Production/main validated commit: `ba936314b6e7547cc66a67566412a33a098e13eb`
Hotfix commit included: `dbdf420cee6fb73871da5410673f36adab6bcc8f`
Work package: `F6.8-02-HF1 — Governed Observation Emitter Timestamp Serialization Runtime Hotfix`

## Final status

`F6_8_02_HF1_RUNTIME = PASS`

`F6_8_02_RUNTIME = PASS`

`F6_8_02 = CLOSED`

`6.8-03 = READY`

## Runtime evidence accepted

The production outbox contained four eligible events previously blocked by PostgreSQL error `22007` because a JavaScript `Date` crossed the emitter-to-Semantic-Layer boundary as a non-ISO string.

After deployment of the HF1 timestamp normalization, the same four failed events were reprocessed through the existing governed outbox consumer. No rows were deleted, rewritten, or manually converted into canonical observations.

All four events transitioned successfully:

| Event | Previous status | Final status | Attempts | Canonical observation |
|---|---|---|---:|---|
| `dad691ca-8b7b-42fb-94ab-a0e3f9408b46` | `failed` / `22007` | `completed` | 2 | `b8ff7fc0-8d77-4a0e-9cd0-0f1b93df25df` |
| `177653d3-e0fa-43b2-9032-f259321ebf27` | `failed` / `22007` | `completed` | 2 | `1b6cbedf-ff8f-4013-86c0-30593a09a8e1` |
| `99b72bec-1d12-428e-a521-aa8ff32f108b` | `failed` / `22007` | `completed` | 2 | `221cad4a-7792-4de0-8f02-a25f9be49f6c` |
| `8706bcf3-7c5a-4603-aaf9-28cb3dad12ae` | `failed` / `22007` | `completed` | 2 | `d3d3d984-5ed7-4f31-a2f4-782fd448fbeb` |

For all four rows:

- `last_error_code` is cleared.
- `processed_at` is populated.
- `observation_id` is populated.
- `reused = false` on the successful retry.
- The original governed outbox rows remain the processing source of truth.

## Canonical Observation persistence

Each completed event joins successfully to `grc_observations` using both `observation_id` and `tenant_id`.

Validated runtime properties:

- `observation_type = official_calculation.data_trust_attention`.
- `entity_type = official_formula`.
- `status_value = open`.
- `severity_value` is material-signal dependent (`low` or `medium`) and was not fabricated from a generic default.
- `observed_at = 2026-08-18 16:51:19.728+00`, proving the instant was preserved through ISO-8601 normalization.
- `is_current = true` for the newly materialized canonical versions.
- `source_snapshot_id` is populated for every validated observation.

## Idempotency and canonical uniqueness

Runtime duplicate checks returned zero rows for:

1. Duplicate `(tenant_id, idempotency_key)` values in `grc_observation_emission_outbox`.
2. More than one current canonical observation for `(tenant_id, source_identity_hash)`.

Therefore the retry did not create duplicate outbox events or parallel current Observation truth.

## Architecture preserved

The closure does not modify or reopen the architectural decisions already closed in 6.8-01/HF1/HF2:

- `grc_observations` remains the canonical Observation table.
- `grc_observation_relations` remains the canonical relation table.
- `grc_observation_links` is not a runtime model.
- Semantic Layer remains the only canonical writer/supersession owner for `grc_observations`.
- The governed emitter/outbox remains an integration mechanism, not a second Observation source of truth.
- PUI / Math Governance source ownership, formulas and Data Trust v1 remain closed.

## Gates

```text
ROOT_CAUSE_TIMESTAMP_SERIALIZATION = PASS
OBSERVATION_TIMESTAMP_NORMALIZATION = PASS
OBSERVATION_TIMESTAMP_RUNTIME = PASS
NO_FAKE_DATES = PASS
GOVERNED_OBSERVATION_EMITTER = PASS
OBSERVATION_OUTBOX_OR_EQUIVALENT = PASS
OBSERVATION_RETRY_SAFETY = PASS
OBSERVATION_IDEMPOTENCY = PASS
OBSERVATION_CANONICAL_PERSISTENCE = PASS
OBSERVATION_PROVENANCE = PASS
OBSERVATION_DATA_TRUST = PASS
OBSERVATION_SUPERSESSION = PASS
CANONICAL_OBSERVATION_ARCHITECTURE = PASS
PARALLEL_OBSERVATION_MODEL = 0
PARALLEL_RELATION_MODEL = 0
DUPLICATE_OUTBOX_IDEMPOTENCY_KEYS = 0
DUPLICATE_CURRENT_SOURCE_IDENTITIES = 0
SELLABLE_MULTI_TENANT = PASS
IMPLEMENTATION_DEBT = NONE
RUNTIME_DEBT = NONE
F6_8_02_HF1_RUNTIME = PASS
F6_8_02_RUNTIME = PASS
F6_8_02 = CLOSED
6.8-03 = READY
```

## Operator note

During manual replay, `source .env` emitted shell parsing warnings for unrelated environment values containing spaces/URL-like tokens. This did not affect the replay because the database variables required by `backend/src/config/db.js` were loaded and all four events completed successfully. Future operator commands should prefer the application's normal environment loader or a targeted dotenv preload instead of treating the complete `.env` file as shell syntax.

## Do not rediscover

- Do not reopen F6.8-01 Observation ownership.
- Do not recreate `grc_observation_links`.
- Do not add direct producer writes to `grc_observations`.
- Do not replace Semantic Layer supersession with mutable updates.
- Do not reopen PUI, formula semantics, Math Governance source contracts or Data Trust v1 without new objective evidence.
- Do not treat historical `22007` outbox rows as an open defect after this runtime closure; the four known rows were successfully retried and materialized canonically.

## Next exact action

Start `6.8-03 — GRC Gap Model` from current `main` and perform mandatory discovery before implementation. The Gap model must be deterministic over canonical observations/rules, must not duplicate domain findings, and must keep AI hypotheses separate from deterministic gap truth.
