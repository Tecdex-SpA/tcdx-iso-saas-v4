# 6.9-03 — Priority Engine 2.0 Runtime Closure

Fecha: 2026-08-19
Estado: CLOSED / PASS_RUNTIME
Runtime main SHA: `cb8b0d853521971b0d4d0f2768c9e3b8c967102a`
Implementation commit: `a11094f2d7ac2ee97330bc5a36ee45464014d02b`

## Evidencia aceptada

El responsable del proyecto confirmó validación post-merge/post-deploy sobre `main` y entregó evidencia runtime desde `bk-v4`.

### Git / baseline

```text
main = cb8b0d853521971b0d4d0f2768c9e3b8c967102a
Merge F6.9-03 Priority Engine 2.0
implementation = a11094f2d7ac2ee97330bc5a36ee45464014d02b
```

### Regresiones post-deploy

```text
priorityEngine.service tests: OK
impactGraph.service tests: OK
grcGap.service tests: OK
grc.service tests: OK
Imports explicit RBAC: VERIFIED routes=10 definitions=33 operational=10 blocked=23
```

### Superficie runtime verificada

Priority Engine queda integrado en la fachada GRC mediante:

- `listPriorities({ tenantId, filters })`
- `getPriority({ tenantId, entityType, entityId, filters })`
- `GET /api/grc/priorities`
- `GET /api/grc/priorities/:entityType/:id`

El contrato de runtime conserva:

```text
model_version = priority-engine-2-v1
subject v1 = grc_gap
source models = grc_gaps + impact-graph-2-foundation-v1
AI priority truth = false
```

### Ausencia de source of truth paralelo

Consulta PostgreSQL post-deploy:

```text
priority/priority_engine tables = 0
```

No se creó tabla de Priority Engine ni persistencia canónica paralela de scores.

### Protección de Observation / relaciones / Gap

Consulta PostgreSQL post-deploy confirmó exclusivamente:

```text
grc_gaps
grc_observation_relations
grc_observations
```

`grc_observation_links` permanece ausente.

## Interpretación de cierre

La evidencia demuestra que 6.9-03 está mergeado y operativo sin alterar los owners canónicos ya cerrados:

- Gap sigue siendo `grc_gaps`.
- Impact Graph sigue siendo una proyección/adapters, no almacenamiento gráfico.
- Priority Engine es cálculo/proyección determinística, no sistema de registro.
- No existe source of truth nuevo de prioridad.
- No existe relation model paralelo.
- No existe Observation model paralelo.
- RBAC y servicios focales permanecen sin regresión observable en las pruebas entregadas.

La puntuación, factores, bandas y orden siguen gobernados por `priority-engine-2-v1`; `evaluated_at` es técnico y no concede autoridad temporal al score. IA/LLM no es autoridad de score.

## Gates runtime

```text
F6_9_03_RUNTIME = PASS
F6_9_03 = CLOSED
PRIORITY_ENGINE_2 = PASS
PRIORITY_SCORE_VERSIONED = PASS
DETERMINISTIC_PRIORITY = PASS
PRIORITY_EXPLAINABILITY = PASS
PRIORITY_PROVENANCE = PASS
IMPACT_GRAPH_REUSE = PASS
GAP_MODEL_REUSE = PASS
AI_PRIORITY_TRUTH = 0
LLM_SCORE_AUTHORITY = 0
NEW_PRIORITY_SOURCE_OF_TRUTH = 0
PARALLEL_OBSERVATION_MODEL = 0
PARALLEL_RELATION_MODEL = 0
IMPLEMENTATION_DEBT = NONE
RUNTIME_DEBT = NONE
```

## Do not rediscover

- No reabrir PUI/Math Governance sin evidencia objetiva nueva.
- No reabrir Observation, Gap, relationship inventory ni Impact Graph por 6.9-03.
- No crear tabla de prioridades para persistir el score v1.
- No usar IA/LLM para producir score, factores, band o ranking determinístico.
- No recrear `grc_observation_links`.
- No duplicar Impact Graph ni `grc_gaps` para alimentar prioridad.

## Siguiente acción exacta

`6.10-01 — Modelo de documentos de conocimiento` queda READY sobre `main` actualizado. Debe extender Knowledge Base v2 y respetar el mecanismo de continuidad/FOCUSED_MINIMAL del Plan Maestro.

## Final verdict

```text
F6_9_03_RUNTIME = PASS
F6_9_03 = CLOSED
IMPLEMENTATION_DEBT = NONE
RUNTIME_DEBT = NONE
6.10-01 = READY
```
