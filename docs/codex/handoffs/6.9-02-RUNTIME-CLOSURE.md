# 6.9-02 — Runtime Closure

Fecha: 2026-08-19
Work package: `6.9-02 — Impact Graph 2.0 Foundation`
Repositorio: `Tecdex-SpA/tcdx-iso-saas-v4`
Runtime base validada: `main@ddd97e02fe7b0c536ab3c3345c2d8d4453febb55`
Implementación mergeada: `2df4f8cb74cc7b9d0b2e050b13fdd25995734683`
Estado: `CLOSED / PASS_RUNTIME`

## Objetivo del cierre

Cerrar la validación runtime de Impact Graph 2.0 Foundation después del merge de `6.9-02`, confirmando que la capa opera como proyección determinística tenant-scoped sobre fuentes GRC existentes, sin graph storage paralelo ni segundo source of truth.

## Evidencia runtime aceptada

### Regresión focal

Ejecutado sobre `bk-v4`:

```text
impactGraph.service tests: OK
grc.service tests: OK
Imports explicit RBAC: VERIFIED routes=10 definitions=33 operational=10 blocked=23
```

El working tree runtime quedó sin cambios reportados por `git status --short`.

### Proyección real Observation -> Gap

Seed validada:

```text
tenant_id = 76c44a0e-6041-8bda-99c7-b740fccea001
observation_id = d3d3d984-5ed7-4f31-a2f4-782fd448fbeb
gap_id = 04f62e96-9fd0-4431-8245-59ce07ce8181
```

Resultado runtime:

```text
model_version = impact-graph-2-foundation-v1
relationships.nodes = 2
relationships.edges = 2
observation_neighborhood.depth = 3
observation_neighborhood.nodes = 2
observation_neighborhood.edges = 2
gap_neighborhood.nodes = 2
gap_neighborhood.edges = 2
```

Se confirmaron dos aristas legítimas Observation -> Gap sin duplicar verdad:

1. `derives_gap`
   - `source_of_truth = grc_gaps`
   - `persisted_or_derived = derived`
   - `derivation_rule = observation.data_trust_attention_gap@1`
   - `owner = grcGap.service.js`

2. `supports`
   - `source_of_truth = grc_observation_relations`
   - `persisted_or_derived = persisted`
   - `owner = semanticLayer.service.js`

Gates runtime reportados por la proyección:

```text
model_version = true
tenant_scope = true
limits = true
observation_gap_found = true
provenance_complete = true
```

### Determinismo

Dos proyecciones idénticas con `projectedAt` fijo generaron el mismo hash y el mismo orden/identidad de nodos y aristas:

```text
hash_a = 169d4599d16ed71dd0b185c9ee25a133ec44ac89b87acc1aa2ab902c8bc9b41f
hash_b = 169d4599d16ed71dd0b185c9ee25a133ec44ac89b87acc1aa2ab902c8bc9b41f
same_projection = true
same_nodes = true
same_edges = true
```

### Aislamiento multi-tenant

Se consultó el mismo Observation ID desde un tenant distinto:

```text
requesting_tenant = 697eefa4-3b56-4c8a-a7d4-6d512c40233e
nodes = 1
edges = 0
foreign_edges = 0
```

No hubo fuga de relaciones ni datos del tenant propietario.

### No graph storage paralelo

La validación previa del runtime confirmó ausencia de tablas dedicadas `impact_graph`, `graph_node` o `graph_edge`. La foundation permanece como proyección/adapters sobre las fuentes definidas en `docs/architecture/grc_relationship_inventory.md`.

## Arquitectura confirmada

```text
DOMAIN SOURCES OF TRUTH
  -> DOMAIN ADAPTERS
  -> impactGraph.service.js
  -> deterministic GraphProjection
  -> GRC API consumers
```

Se mantiene:

- `grc_observation_relations` como relación canónica de Observation.
- `grc_gaps` como verdad determinística de Gap.
- relaciones de dominio como sources of truth.
- `impactGraph.service.js` como capa de lectura/proyección, no sistema de registro.

## Cambios funcionales en este cierre

```text
FUNCTIONAL_CODE_CHANGED = NO
MIGRATIONS_CHANGED = NO
FORMULAS_VERSIONED = []
MATH_GOVERNANCE_SOURCE_CONTRACTS_VERSIONED = []
SEMANTIC_CONTRACTS_VERSIONED = []
```

Este work package de cierre es documental únicamente.

## Gates finales

```text
F6_9_02_RUNTIME = PASS
F6_9_02 = CLOSED
IMPACT_GRAPH_FOUNDATION = PASS
GRAPH_PROJECTION_CONTRACT = PASS
RELATIONSHIP_ADAPTERS = PASS
GRAPH_DETERMINISM = PASS
GRAPH_PROVENANCE = PASS
GRAPH_TEMPORAL_SEMANTICS = PASS
GRAPH_TENANT_ISOLATION = PASS
MULTI_TENANT_ISOLATION = PASS
SELLABLE_MULTI_TENANT = PASS
DUPLICATE_RELATION_MODEL = 0
PARALLEL_RELATION_MODEL = 0
PARALLEL_OBSERVATION_MODEL = 0
NEW_GRAPH_SOURCE_OF_TRUTH = 0
IMPLEMENTATION_DEBT = NONE
RUNTIME_DEBT = NONE
```

## Do not rediscover

- No rehacer `6.9-01` ni el inventario de relaciones.
- No crear `grc_graph_edges`, Neo4j ni otra persistencia graph como segundo source of truth para esta foundation.
- No recrear `grc_observation_links`.
- No cambiar ownership canónico de Observation, Gap ni sus relaciones.
- No convertir IA/LLM en autoridad de score o verdad determinística del grafo.
- No reabrir PUI, Math Governance ni F6.8 sin evidencia objetiva nueva.
- `6.9-03` debe consumir `impactGraph.service.js` y la provenance ya cerrada; no duplicar traversal ni relaciones.

## Siguiente work package

`6.9-03 — Priority Engine 2.0` queda habilitado.

Objetivo rector según Plan Maestro: evolucionar next-best-actions desde ordenamiento por severidad hacia prioridad determinística, versionada y explicable multi-factor, consumiendo Impact Graph cuando corresponda y preservando la lógica actual como compatibility/fallback adapter.

```text
6.9-03 = READY
PRIORITY_ENGINE_2_0 = NEXT
```
