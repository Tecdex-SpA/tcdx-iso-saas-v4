# DECISIONS — TCDX ISO SaaS V4

Las siguientes decisiones provienen del Plan Maestro y se consideran `VERIFIED`.

| ADR | Estado | Decisión |
|---|---|---|
| ADR-001 | VERIFIED | No LLM direct SQL. El LLM recibe contextos autorizados. |
| ADR-002 | VERIFIED | PostgreSQL outbox first. Kafka sólo con necesidad/benchmark demostrado. |
| ADR-003 | VERIFIED | PostgreSQL pgvector first. Vector DB separada sólo con escala demostrada. |
| ADR-004 | VERIFIED | PostgreSQL graph abstraction first. Neo4j no es requisito inicial. |
| ADR-005 | VERIFIED | Extend Knowledge Base v2. No crear una segunda KB. |
| ADR-006 | VERIFIED | Extend Intelligence Engine. No crear un segundo orchestrator transversal. |
| ADR-007 | VERIFIED | Deterministic truth, AI explanation. Scores/gaps oficiales no los inventa el LLM. |
| ADR-008 | VERIFIED | Tenant learning via memory/RAG. No fine-tuning cross-tenant automático. |
| ADR-009 | VERIFIED | Authoritative regulatory sources. Web general no es Source of Truth legal. |
| ADR-010 | VERIFIED | `CODEX_VALIDATION_MODE = FOCUSED_MINIMAL`; CI/merge/deploy son manuales. |
| ADR-071 | VERIFIED_LOCAL | GRC post-mutation orchestration is an adapter, not a second calculation authority: producers may request affected functional metric codes, but execution remains `indicatorGovernance -> officialCalculationOrchestrator`. Compliance mappings in `grc_requirement_control_mappings` are governed normative relations and must not be invented by Diagnostico/SoA. SoA/Diagnostico record `control_soa_assessments` history, use `suggested_implementation_status`/`suggested_applicable` as math signals, and read latest assessment deterministically for fallback. The adapter must not invent `grc_control_assurance` 100/50/0 or unsupported assurance statuses; without a governed conversion contract it leaves assurance projection skipped and observable. Post-COMMIT publication failures must return inside `official_recalculation` instead of false mutation failure. |
| ADR-011 | VERIFIED | Temporalidad oficial de Math Governance debe ser contract-driven: `temporal_semantics` define campo canónico, significado, período, `as_of`, timezone y política de ausencia; `created_at`/`updated_at` no son fallback universal. |
| ADR-012 | VERIFIED | Status oficial de Math Governance debe ser contract-driven y domain-aware: `status_semantics` define dominio, mapping versionado, política de unknown visible y elegibilidad; no existe diccionario universal ni fallback silencioso a estados válidos. |
| ADR-013 | VERIFIED | Fallback legacy en Math Governance debe estar gobernado por el resolver: sólo `primary_absent` o `primary_no_rows` con contrato explícitamente autorizado pueden activar fallback; incompatibilidad, contrato inválido, filas excluidas, status/temporal/scale invalid no activan fallback. |
| ADR-014 | VERIFIED | Data Trust oficial de Math Governance es determinístico y versionado (`data-trust-model-v1`): consume señales PUI-01..PUI-06, separa confianza del valor de métrica, distingue insuficiencia de baja confianza y no usa IA. |
| ADR-015 | VERIFIED | El pipeline oficial único para fórmulas gobernadas es `officialCalculationOrchestrator -> sourceResolver -> source contracts/validation/Data Trust -> persisted calculation/snapshot`. `phase5_5_package3` no puede calcular ni persistir verdad paralela; sólo puede operar como capa de compatibilidad/proyección. |
| ADR-016 | VERIFIED | Los vocabularios legítimos producidos por dominios F5_5 deben reconciliarse contra `statusSemantics.service.js` mediante contratos por dominio y drift guard automatizado; `valid_to` futuro en `validity_interval` no es un evento futuro inválido, mientras que `event_stream`/`valid_from` futuro sigue excluido según contrato temporal. |
| ADR-017 | VERIFIED | El cierre residual HF3 mantiene semántica por productor: findings snapshot de readiness no tienen status operacional propio y usan `not_applicable` más temporalidad del snapshot padre; maturity acepta sólo vocabulario productor conocido; health components pueden validar con `started_at/completed_at` cuando `calculation_runs.period_start` es nulo. |
| ADR-018 | VERIFIED | Para `F5_5_SEVERITY_INDEX`, el source ownership publicado prevalece sobre `source_overrides`/`body.source_code`: `audit_findings_actions` es el source code canónico y `incident_operational_events/grc_incidents` no puede producir verdad paralela ni falso `SOURCE_SCHEMA_INCOMPATIBLE`; el override no canónico queda visible como warning/provenance. HF4 no generaliza esta política a fórmulas con overrides/indicadores legítimos ya existentes. |
| ADR-019 | VERIFIED | Para `F5_5_SEVERITY_INDEX`, el physical source canónico es `grc_readiness_findings` unido a `grc_readiness_snapshots`; el schema productor de snapshots tiene `generated_at`, `period_start` y `period_end`, pero no `source_as_of`. El contrato `audit_findings_actions` v9 y su adapter no pueden requerir ni fabricar `source_as_of`; una ausencia real de findings canónicos queda como dataset vacío/no calculable, no como portfolio de severidad en cero. |
| ADR-020 | VERIFIED | PUI-08 establece la matriz oficial computable como artefacto de cierre PRE-UI: las 53 fórmulas de `FORMULAS` se derivan de `FORMULA_SOURCE_MAP`, source contracts, consumers, dependencias, snapshot/lineage y escenarios empty/partial/sufficient/two-tenant. La matriz no es una lista manual ni habilita motores paralelos; consumers sólo pueden proyectar resultados oficiales persistidos o ejecutar el `officialCalculationOrchestrator`. |
| ADR-021 | VERIFIED | PUI-09 cierra formalmente la fase PUI y declara `PRE_UI_DATA_TRUTH_GATE=PASS` con evidencia runtime productiva post-PUI-08: no hay `calculated` con fuentes incompatibles/no disponibles, los calculated tienen snapshot, los datasets poblados tienen lineage, `not_calculable` no fabrica cero/completed, Data Trust v1 está presente y Severity Index conserva su fuente canónica. La siguiente fase autorizada es Fase 6 ampliada desde F6.8/6.8-01. |
| ADR-022 | VERIFIED | 6.8-01-HF1 corrige 6.8-01: el modelo canónico transversal de observaciones GRC ya existía en el Semantic Layer. `grc_observations` es el system of record canónico, `grc_observation_relations` es el modelo canónico de relaciones, `semanticLayer.service.js` es owner/runtime de persistencia, y `grcObservation.service.js` es fachada GRC. La observación canónica es append-only salvo supersession controlado, tenant-scoped, con `status_value`/`severity_value`, source snapshots/provenance/lineage y sin modelo paralelo. |
| ADR-023 | VERIFIED | 6.8-01-HF2 corrige el bootstrap runtime faltante de `grc.manual_observations`: el contrato semántico de observaciones manuales debe existir mediante forward migration registrada en `schema_migrations`, global (`tenant_id=NULL`), `published`, con v1 `published` y `current_version_id` apuntando a esa v1. Runtime closure post-deploy confirmó `SEMANTIC_CONTRACT_GRC_MANUAL_OBSERVATIONS=PASS_RUNTIME`, sin duplicados ni contratos tenant-specific. No se permite depender de re-ejecutar una migración histórica ni crear contratos tenant-specific con el mismo `source_code`. |
| ADR-024 | VERIFIED | 6.8-02 implementa PostgreSQL outbox para emisión gobernada de Observations sin modelo paralelo: `grc_observation_emission_outbox` almacena eventos tenant-scoped, idempotentes y reintentables; el productor inicial es `officialCalculationOrchestrator`; el consumer usa exclusivamente `semanticLayer.createManualObservation` para persistir en `grc_observations`. La regla inicial `official_calculation.data_trust_attention@1` sólo emite resultados `calculated` con Data Trust material (`TRUSTED_WITH_WARNINGS` o `LOW_CONFIDENCE`) y `observed_at` explícito; no-data, insufficient, source-incompatible, dependency-pending y fallas de fuente quedan no elegibles/ignored y no generan observaciones negativas. |
| ADR-025 | VERIFIED | 6.8-02-HF1 fija el contrato temporal del boundary Outbox -> Semantic Layer: las columnas `timestamptz` del outbox pueden llegar a Node.js como `Date`, pero `grcObservationEmitter.service.js` debe pasar `observed_at`, `period_start` y `period_end` a `semanticLayer.createManualObservation` como strings ISO-8601 UTC. `observed_at` requerido inválido/faltante no se reemplaza por `now()`; `period_start`/`period_end` pueden permanecer `null`; no hay migración, no hay cambios de fórmulas/source contracts y 6.8-02 runtime fue validado post-deploy. |
| ADR-026 | VERIFIED | 6.8-03 introduce un GRC Gap Model canónico aditivo sobre Observations: `grc_gaps` representa brechas determinísticas tenant-scoped derivadas de `grc_observations`; `grc_gap_rules` versiona reglas publicadas inmutables; `grc_gap_status_history` conserva lifecycle; `grc_gap_hypotheses` separa hipótesis AI/no determinísticas. Findings/readiness/nonconformities/action plans no se convierten silenciosamente en Gap; la relación Observation -> Gap usa `grc_observation_relations`; no se crea modelo paralelo de Observation ni relation. Runtime closure confirmó `F6_8_03_RUNTIME=PASS`. |
| ADR-027 | VERIFIED_LOCAL | 6.9-01 establece que Impact Graph 2.0 debe partir de `docs/architecture/grc_relationship_inventory.md` y proyectar/adaptar relaciones propietarias existentes antes de crear persistencia nueva. No se crea `grc_graph_edges` ni tabla equivalente en 6.9-01; `grc_observation_relations`, `grc_gaps`, relaciones de evidencia/auditoría/proveedor/proceso/métrica y `data_lineage_edges` conservan su ownership. Duplicate relation model = 0. |
| ADR-028 | VERIFIED | 6.9-02 implementa Impact Graph 2.0 como contrato de proyección/adapters (`impact-graph-2-foundation-v1`) sobre sources existentes: IDs determinísticos para nodos/aristas, provenance source-record, persisted/derived explícito, tenant guard y límites de traversal. No se crea graph storage, migración, Neo4j, ni segundo source of truth; Observation -> Gap consume `grc_observation_relations` y `grc_gaps` sólo aporta provenance derivada. Runtime closure confirmó `F6_9_02_RUNTIME=PASS`. |
| ADR-029 | VERIFIED | 6.9-03 implementa Priority Engine 2.0 como contrato `priority-engine-2-v1` de evaluación/proyección reproducible sobre `grc_gaps` e Impact Graph 2.0. No se crea tabla de prioridad ni source of truth paralelo; `evaluated_at` es tiempo técnico y no participa del score; IA puede explicar pero no producir score, factores u orden determinístico. Runtime closure confirmó `F6_9_03_RUNTIME=PASS`, `LLM_SCORE_AUTHORITY=0` y `NEW_PRIORITY_SOURCE_OF_TRUTH=0`. |
| ADR-030 | VERIFIED | 6.10-01 extiende Knowledge Base v2 con `knowledge_documents` como modelo documental canónico `knowledge-document-model-v1` para `GLOBAL`, `REGULATORY` y `TENANT`; `knowledge_sources.knowledge_document_id` es un enlace aditivo y `knowledge_items`/child tables siguen intactos. No se crea segunda KB, no se auto-promueven attachments operacionales, no se implementan pgvector/embeddings/retrieval/RAG en 6.10-01; versioning, lifecycle, checksums, vigencia y tenant scope quedan protegidos por constraints DB y service boundary. Runtime closure confirmó `F6_10_01_RUNTIME=PASS`. |
| ADR-031 | VERIFIED | 6.10-02 implementa ingestion tenant como contrato `knowledge-ingestion-pipeline-v1` sobre KB v2: upload explícito con `secureUpload.js`, tenant efectivo desde auth, extracción local reutilizando `documentContentExtraction.service.js`, clasificación sensible mínima, chunks determinísticos en `knowledge_document_chunks`, audit en `knowledge_document_ingestion_audit`, idempotencia por tenant/document/version/checksum y linkage por `knowledge_sources.knowledge_document_id`. Runtime closure confirmó `F6_10_02_RUNTIME=PASS`; no se crea segunda KB, no se reutiliza `tenant_evidence_chunks` como truth de knowledge, no se auto-promueven attachments operacionales y no se implementan pgvector/embeddings/retrieval/RAG. |
| ADR-032 | VERIFIED | 6.10-03 implementa `knowledge-embedding-contract-v1` como capa pgvector sobre Knowledge Base v2: `knowledge_chunk_embeddings` es storage embedding-side referenciado a `knowledge_document_chunks`, sin copiar `chunk_text` ni crear segunda verdad de chunks. El provider/model/version/dimensions es configurable/gobernado; `tenant_id` se filtra en SQL antes de ranking vectorial; no se implementa Hybrid Retrieval, RAG answer/citations, reranker ni Vector DB externa. Runtime closure confirmó `F6_10_03_RUNTIME=PASS`. |
| ADR-033 | VERIFIED | 6.10-04 implementa `hybrid-retrieval-contract-v1` como orquestador determinístico sobre `knowledge_document_chunks` y `knowledge_chunk_embeddings`: combina recuperación lexical tenant-scoped y vectorial pgvector reutilizada mediante normalización explícita por rank y pesos versionados. No crea tablas, no duplica chunks/embeddings, no usa LLM para scoring, no implementa RAG answer/citations y expone sólo candidatos/provenance para 6.10-05. Runtime closure confirmó `F6_10_04_RUNTIME=PASS`. |
| ADR-034 | VERIFIED | 6.10-05 implementa `rag-grounded-answer-contract-v1` como proyección runtime sobre Hybrid Retrieval: el LLM sólo genera respuesta estructurada desde contexto evidence-only; citations se ensamblan y validan determinísticamente contra candidatos de `hybrid-retrieval-contract-v1`; evidencia insuficiente produce abstención; no se crea tabla RAG, motor retrieval paralelo, KB v3, segunda verdad de chunks ni autoridad operacional del LLM. Runtime closure `docs/codex/handoffs/6.10-05-RUNTIME-CLOSURE.md` confirmó `F6_10_05_RUNTIME=PASS` sobre `main@d098441ec4deff867820f989d8595cfb3206571b`. |
| ADR-035 | VERIFIED | F6.11-A introduce la foundation regulatoria canónica sin plataforma paralela: `regulatory_authoritative_sources` gobierna fuentes oficiales, `regulatory_ingestions` versiona artefactos regulatorios inmutables enlazados a `knowledge_documents`, `knowledge_document_chunks` se extiende con `scope` para chunks regulatorios, y `regulations`/`regulation_versions`/`legal_obligations` modelan identidad legal, versión y obligación explícita. No se crean `regulatory_documents_v2`, `regulatory_chunks`, segunda KB, segunda tabla de embeddings ni autoridad legal del LLM. Runtime closure confirmó `F6_11_A_RUNTIME=PASS`. |
| ADR-036 | VERIFIED | F6.11-B implementa Semantic Diff y Regulatory Packs como modelos gobernados/versionados sobre la verdad regulatoria existente: `regulatory_semantic_diffs` y `regulatory_semantic_diff_changes` guardan diff determinístico entre versiones de la misma regulación; `regulatory_obligation_change_lineage` preserva historia de obligaciones; `regulatory_packs`/`regulatory_pack_versions`/`regulatory_pack_items` componen fuentes, regulaciones, versiones, obligaciones y diffs sin copiar texto legal; `regulatory_pack_tenant_activations` y `regulatory_pack_applicability_*` separan configuración/evaluación tenant de la definición global. Runtime closure `docs/codex/handoffs/6.11-B-RUNTIME-CLOSURE.md` confirmó migración, postconditions, tests focales y ausencia de modelo paralelo. IA puede explicar o sugerir draft, pero no publicar verdad legal, obligaciones ni aplicabilidad confirmada. No se crea KB/chunks/embeddings/retrieval/RAG paralelo. |
| ADR-037 | VERIFIED | F6.12-A implementa Intelligence derivada como proyeccion runtime versionada y cerrada en runtime por `docs/codex/handoffs/6.12-A-RUNTIME-CLOSURE.md`: `canonical-intelligence-context-v1` unifica contexto tenant-scoped y separa facts, derived_signals, retrieved_knowledge, regulatory_context, historical_context, missing/insufficient; `pattern-trend-engine-v1` y `anomaly-engine-v1` calculan senales deterministicas con guards de muestras; `cross-grc-intelligence-orchestrator-v1` reutiliza Priority Engine 2, Impact Graph, grounded RAG y regulatory context sin crear source of truth paralelo. AI puede explicar/sintetizar contexto acotado, pero no producir verdad operacional, score, anomaly numerica, Gap, Observation, Priority ni SQL directo. |
| ADR-038 | VERIFIED | F6.13-A implementa Operational Learning como historia gobernada y tenant-scoped, no como nueva verdad operacional: `recommendation_decision_ledger` registra decisiones humanas append-only sobre recomendaciones/contexto/prioridad; `recommendation_effectiveness_evaluations` compara before/action/after/expected/observed sin asumir closed=effective; `operational_memory_cases` y `operational_memory_case_links` modelan memoria consultable separando facts, decisions, outcomes, evaluations, confirmed lessons y AI hypotheses. Runtime closure `docs/codex/handoffs/6.13-A-RUNTIME-CLOSURE.md` confirmo migracion, tests, deploy runner y ausencia de truth paralelo. Reutiliza `iso_operational_suggestions`, `iso_recommended_action_conversions`, `audit_event_log`, Cross-GRC Intelligence, Priority Engine 2 y KB/RAG por referencia/contrato; AI no decide, no evalua efectividad como autoridad, no publica memoria confirmada y no ejecuta SQL directo. |
| ADR-039 | VERIFIED_LOCAL | F6.14-A formaliza AI Governance y AI Evaluation sin crear un segundo AI orchestrator ni AI truth store: `ai-governance-contract-v1` y `ai-capability-registry-v1` declaran provider/model/prompt/context/schema/policy/grounding/authority/tenant/privacy/failure boundaries por capacidad real; `ai-evaluation-suite-v1` usa fixtures sinteticos gobernados y thresholds versionados para validar estructura, citas, factualidad, insuficiencia, prompt injection, tenant isolation, fallback y regresion entre versiones. El LLM no puede ejecutar SQL, publicar verdad operacional/legal, decidir, aceptar riesgo, cerrar gaps ni confirmar memoria. Fase 6 ampliada queda cerrada localmente con runtime pendiente por validacion manual. |
| ADR-040 | VERIFIED_LOCAL | RBAC-01 establece que la compatibilidad de roles no es alias de privilegios: el sistema resuelve familia canónica y conserva `effective_role` legacy para permisos. La autorización efectiva requiere permiso de usuario, entitlement comercial, módulo activo y scope cuando aplique. `admin != tenant_admin`, `superadmin != platform_admin` y `operativo -> area_owner` se preservan como compatibilidad documentada; no se reasignan usuarios ni se ejecuta migración productiva desde Codex. |
| ADR-041 | VERIFIED_LOCAL | RBAC-02 establece una excepción comercial estricta sólo para `core.dashboard`: tenants comerciales activos con suscripción activa/trialing/past_due pueden recibir capability base `core.dashboard` aunque falte la fila técnica de módulo `core`, siempre bajo permiso `dashboards.read`. No existe fallback genérico para módulos ausentes, no se sobreescribe un deny explícito y tenants/suscripciones inactivas no reciben la capability base. La auditoría READ-ONLY real encontró `dashboard_module_mismatches=0`, `false_denies=0` y un false ALLOW risk pre-fix por `core.dashboard.required_permission=commercial.entitlement.read`; RBAC-02 lo corrige en runtime y requiere migración forward-only de catálogo. |
| ADR-042 | VERIFIED_LOCAL | RBAC-03 queda `NOT_REQUIRED` para reconciliación de roles/permisos por nuevo hecho confirmado: Admin Credex recuperó Dashboard al refrescar contrato desde superadmin. La causa primaria es sincronización/frescura de contrato comercial, no falta de `dashboards.read`. `tenant_contracts` y `tenant_subscriptions` deben sincronizarse automáticamente al guardar contrato Admin SaaS; `/api/me/entitlements` debe validar tenant efectivo y frontend debe invalidar caches por cambio de usuario/token/tenant. No hay migración RBAC, grants nuevos ni bypass por tenant/usuario. |
| ADR-043 | VERIFIED_LOCAL | El modelo comercial estándar usa alias no destructivos sobre `plan_key` persistidos: `iso -> pyme -> ISO`, `iso_operational_risk -> empresa -> ISO + Riesgo Operativo` y `grc -> enterprise -> GRC`. La autoridad de módulos/capabilities permanece en `commercial_plans`, `commercial_plan_versions`, `plan_version_modules`, `commercial_modules`, `commercial_technical_capabilities` y vistas `v_commercial_*`; frontend sólo muestra planes y módulos derivados desde backend. La matriz publicada debe normalizarse por migración comercial forward-only `20260828_commercial_standard_plan_matrix` sin tocar roles, usuarios, contratos históricos ni datos operacionales. |
| ADR-044 | VERIFIED_LOCAL | La matriz comercial definitiva es capability-based y funcional: `ISO = ONLY_ISO`, `ISO_RISK = ISO + OPERATIONAL_RISK_ONLY`, `GRC = ALL_TENANT_COMMERCIAL_CAPABILITIES`. `grc_core`/`operations_grc`/`risk_manager` no son proxies comerciales suficientes si mezclan dominios; los módulos se derivan de capabilities exactas. La migración `20260828_commercial_standard_plan_matrix` materializa capabilities ISO semánticas faltantes, valida sobre/subexposición antes de cambiar plan-version modules y no toca RBAC, scope, usuarios, tenants, contratos ni datos operacionales. |
| ADR-045 | VERIFIED_LOCAL | AI-ADDON-01 corrige el modelo comercial de IA hacia adelante: `ai.compliance` y `ai.auditor` no son entitlement efectivo de ningun plan base, incluido GRC, sin `tenant_subscription_addons.addon_key='ai'` activo y no vencido. La migración historica `20260828_commercial_standard_plan_matrix` no se reescribe; `20260831_ai_addon_commercial_visibility` publica el add-on transversal, retira `ai_compliance` de plan-version modules estandar publicados y recrea vistas comerciales para source `addon`. Runtime AI flags son subordinados al contrato y no pueden bypassear add-on, RBAC ni scope. |
| ADR-046 | VERIFIED_LOCAL | AI-ADDON-02 cierra el modelo binario comercial de IA: `tenant_subscription_addons.addon_key='ai'` es la unica autoridad contractual de IA; `tenants.ai_plan` permanece solo como compatibilidad historica sin capacidad de conceder o denegar acceso, no se muestra ni se selecciona en Admin SaaS, y no participa en `/api/me/entitlements`, `isTenantAiFeatureEnabled`, Sidebar/AppLayout ni gates runtime. Reactivar el add-on restaura IA sin cambio de plan base ni refresh contractual manual; suspender/cancelar/inexistente deniega IA aunque `ai_enabled`, `ai_features_json` o `ai_plan` indiquen lo contrario. |
| ADR-047 | VERIFIED_LOCAL | NORMALIZATION-01 establece las autoridades definitivas DB/backend para los defectos postdeploy confirmados: `ai.compliance` usa `ai.view` como permiso canonico de lectura y sigue requiriendo add-on IA activo; `iso.actions` usa `actions.view` como permiso canonico de lectura y permisos especificos `actions.manage/actions.approve/actions.delete` para mutaciones; no se crean aliases `ai_compliance.read`/`actions.read`. La migracion forward-only cierra duplicados efectivos de `tenant_subscription_addons.ai`, retira IA plan-level de versiones publicadas y valida que IA efectiva venga solo de add-on. `GET /api/grc/overview` es lectura de ultimos calculos/snapshots persistidos y no ejecuta `recalculateOfficialAnalytics`; el recalculo queda en endpoints POST explicitos. |
| ADR-048 | VERIFIED_LOCAL | NORMALIZATION-02 establece una sola autoridad ejecutiva para Health/KPI: `official_formula_versions + calculation_runs/results + metric_snapshots + metric_source_bindings`. `F5_5_GRC_HEALTH` v1 estricta se conserva historicamente y v2 gobierna disponibilidad parcial con componentes `AVAILABLE/MISSING/NOT_APPLICABLE/NOT_CONFIGURED/STALE/INVALID/UNKNOWN`, denominador dinamico solo para `AVAILABLE`, exclusion solo de `NOT_APPLICABLE`, cobertura/confianza explicita y publicacion ejecutiva solo si `coverage >= minimum_coverage`. `DATA-TRUST` sin `accuracy` real queda `NOT_CONFIGURED` y no bloquea todo el score; `EVIDENCE-FRESH` y `COVERAGE` son metricas oficiales separadas y `EVIDENCE-COVERAGE` queda solo como alias de compatibilidad. `KPI-HLT-*` permanece como componente/fuente de compatibilidad, no como segundo Score Global. |
| ADR-049 | VERIFIED_LOCAL | RELEASE-CLOSEOUT de NORMALIZATION-01/02 no puede avanzar a commit/deploy sin preflight PostgreSQL PASS de ambas migraciones. Con `MIGRATION_DATABASE_URL` ausente, Gates A/B/C locales pueden quedar PASS pero el verdict debe ser `RELEASE_CLOSEOUT_NO_GO`, no GO ni deploy manual improvisado. La siguiente ejecucion debe continuar desde `docs/codex/handoffs/RELEASE-CLOSEOUT-NORMALIZATION.md` con preflight oficial como primer paso. |
| ADR-050 | VERIFIED_LOCAL | HOTFIX-POSTDEPLOY-01 cierra la brecha RBAC de IA Compliance manteniendo `ai.view` como permiso canonico: sólo los roles tenant `admin`, `tenant_admin` y `auditor` reciben `ai.view` mediante migracion forward-only/idempotente; roles plataforma existentes permanecen autorizados y cualquier otro rol activo con `ai.view` hace fallar postconditions. La autoridad comercial IA sigue siendo `tenant_subscription_addons.addon_key='ai'` + capability efectiva + RBAC + runtime flags; `ai_plan` no participa. Health/GRC no pueden consultar `calculation_runs.source_as_of` ni `calculation_runs.created_at`; la temporalidad canónica de esa proyección usa columnas reales `period_end/completed_at/started_at/period_start`. |
| ADR-051 | VERIFIED_LOCAL | DB-N01 establece que la identidad operacional canonica de control en runtime es `tenant_controls.id`; `controls_catalog.id` es identidad de catalogo y `controls.id` queda como compatibilidad legacy historica. Nuevas escrituras runtime de findings, evidences y action plans no pueden persistir `controls.id` como `tenant_control_id`. Lecturas transicionales pueden aceptar IDs legacy solo con resolucion explicita tenant-scoped hacia un unico `tenant_controls`; la ambiguedad requiere decision manual y la migracion forward-only debe bloquearla, no resolverla por orden, `LIMIT 1`, `max()` ni `min()`. Las decisiones manuales DB-N01 usan `table_name`, `record_id`, `selected_tenant_control_id`, son unicas por `(table_name, record_id)` incluso si son identicas y deben validar record existente, tenant, catalogo y contexto operacional antes de cualquier update. |
| ADR-052 | VERIFIED_LOCAL | DB-N02 establece que la autoridad ejecutiva Health unica es `F5_5_GRC_HEALTH` v2 por el pipeline gobernado `official_formula_versions -> calculation_runs -> calculation_outputs -> metric_snapshots -> metric_source_bindings`; `canonicalHealthProjection.service.js` tiene rol `READ_NORMALIZE_EXPLAIN_PRESENT_ONLY` y no ejecuta formula ejecutiva paralela. `control_health_scores`, `v_latest_health_kpi_snapshots` y `KPI-HLT-*` quedan como drill-down/compatibilidad. `refresh_control_health_scores_v2_1(uuid)` reconstruye `standard_code` desde `tenant_controls.control_id -> controls_catalog_standards/controls_catalog.iso -> tenant_standards active`, nunca desde el valor previo de `control_health_scores.standard_code`; multiples normas activas para el mismo tenant control devuelven `AMBIGUOUS` y no se actualizan silenciosamente. `EVIDENCE-FRESH` es freshness, `COVERAGE` es coverage oficial/compliance segun consumidor y `EVIDENCE-COVERAGE` queda alias de compatibilidad. `DATA-TRUST` sin fuente medible permanece `NOT_CONFIGURED` y no se convierte en score numerico. |
| ADR-053 | VERIFIED_LOCAL | DB-N03 endurece integridad multi-tenant con constraints compuestas criticas, no con rollout RLS completo. Las relaciones tenant-scoped criticas (`findings`, `evidences`, `action_plans`, `control_health_scores`) deben referenciar `tenant_controls` por `(tenant_id, tenant_control_id) -> (tenant_id, id)` despues de DB-N01/DB-N02, preservando `tenant_controls.id` como identidad operacional canonica y sin cambiar la semantica Health DB-N02. RLS queda preparado mediante funciones `tcdx_security.*` y politicas piloto staged, pero `ENABLE/FORCE RLS` se difiere hasta que backend garantice contexto tenant transaccional con `SET LOCAL`/`set_config(..., true)` en todos los paths tenant-scoped. Platform admin no debe operar como runtime tenant con `BYPASSRLS`; debe usar scope explicito auditado o conexion administrativa separada. |
| ADR-054 | VERIFIED_LOCAL | DB-N04 establece el helper canonico runtime para contexto DB tenant: todo path Node tenant-scoped debe ejecutar queries bajo contexto transaccional `BEGIN` + `set_config('app.tenant_id', tenant, true)` y nunca depender de GUC persistente de sesion en `pg.Pool`. El pool exportado queda envuelto para `pool.query` y `pool.connect` bajo AsyncLocalStorage; platform scope es un path explicito role-gated que prohibe `tenant_id` y no concede bypass de escritura tenant. `ai_reader` queda `AI_READER_RLS_DEFERRED` hasta que AI Engine use tenant context o vistas tenant-safe probadas. Legacy cleanup no se hace por nombre: `controls`, KPI-HLT y Health legacy operativo siguen compatibilidad, y todo backup/preview queda `DROP_CANDIDATE_DEFERRED` hasta probar 0 readers/0 writers/0 dependencies y aprobacion de retention. |
| ADR-055 | VERIFIED_LOCAL | DB-N05 establece que una base productiva nueva debe crearse desde un baseline consolidado y reproducible, no copiando QA ni ejecutando indiscriminadamente la cadena historica que contiene reparaciones, transiciones, backups y datos demostrativos. `database/baseline/production_schema_v1.sql` absorbe los contratos canonicos DB-N01..DB-N04 para fresh bootstrap; `database/baseline/production_seed_v1.sql` carga solo referencia/sistema global. Revision critica 2026-09-07 corrigio que `dbn02_normalize_health_component_state` no puede promover `UNKNOWN/MISSING/NOT_CONFIGURED/STALE + numeric` a `AVAILABLE`, exige lineage reconstruible source contract -> binding -> formula version -> run -> output -> snapshot, y prohibe grant directo `ai_reader` sobre tenant-bearing `ai_core` mientras `AI_READER_RLS_DEFERRED`. `qa_audit.*`, backups, cleanup backups, previews y datos tenant/customer no pertenecen al baseline fresh. `controls`, `control_health_scores` y `v_latest_health_kpi_snapshots` se retienen por compatibilidad hasta retiro seguro. El gate real queda `DB_N05_PARTIAL` hasta que `RUNTIME_TO_BASELINE_COVERAGE` tenga `ACTIVELY_USED_MISSING=0`, los catálogos productivos sean reproducibles/completos y los grants backend/platform/support sean de mínimo privilegio. |
| ADR-056 | VERIFIED_LOCAL | DB-N05 no permite `GRANT EXECUTE ON ALL FUNCTIONS` para runtime roles. `tcdx_backend_runtime` recibe solo los helpers `tcdx_security.*` y las funciones DB-N02 Health requeridas; `tcdx_platform_runtime` recibe solo helpers `tcdx_security.*`; `tcdx_readonly_support` y `ai_reader` no reciben ejecucion de funciones app-owned. `ALTER DEFAULT PRIVILEGES ... REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC` bloquea herencia publica futura en `public` y `tcdx_security`; toda funcion nueva requiere grant explicito y actualizacion del test efectivo de allowlist. |
| ADR-059 | VERIFIED_LOCAL | TCDX SaaSv2 GRC runtime V3 establece `tenant_module_settings.is_enabled` como autoridad runtime para módulos GRC; `enabled` permanece solo como compatibilidad sincronizada por trigger y las escrituras divergentes se rechazan. `saas_modules` debe incluir `default_enabled`, `is_system` y `updated_at`. El baseline fresh y la migración forward-only V3 deben representar los objetos/columnas GRC Phase 1/2/3 consumidos por backend activo, sin seeds tenant-specific, sin `tcdx.local`, sin `GRANT ON ALL TABLES` y sin `GRANT EXECUTE ON ALL FUNCTIONS`. |
| ADR-060 | VERIFIED_LOCAL | El contrato release RBAC/capability es `usuario -> rol canonico -> permiso RBAC -> tenant scope -> entitlement/capability -> acceso efectivo`. `viewer` es rol canonico separado de `executive`; aliases legacy se normalizan en `roleCompatibility.service.js` sin reescritura destructiva de usuarios. `dealer` mantiene autorizacion especial por asignacion de tenant y permisos minimos de portal, no acceso tenant general. Las autoridades ejecutables legacy de plataforma se centralizaron en `roleCompatibility.service.js`; frontend usa helpers espejo y backend `/api/me/permissions`/`/api/me/modules`/capabilities. Validado localmente con static contract, PostgreSQL aislado, siete roles tenant A/B, dealer assignment, entitlement enforcement, frontend/backend consistency, reapply y checksum mismatch fail-closed. |

| ADR-069 | VERIFIED_LOCAL | Control lifecycle systemic closeout defines effective catalog by ownership scope, not provenance: global catalog is `controls_catalog.tenant_id IS NULL`; tenant catalog is `controls_catalog.tenant_id=current tenant`; `generic` selects active global controls, `personalized` selects active same-tenant controls, and `mixed` selects both while deduping only declared replacements by `COALESCE(base_control_id,id)`. `source_type` remains provenance (`knowledge_base_seed_v2`, `migrated_reference_from_qa`, `personalized`, etc.) and must not be rewritten to satisfy runtime filters. Standard membership comparisons normalize case/whitespace for `controls_catalog.iso` and `controls_catalog_standards.standard_code` without changing persisted codes. `initialize-controls` materializes tenant controls and default applicability idempotently, writes applicability under the requested standard context, preserves human/profile exclusions, keeps scores NULL until real evaluation, and feeds consumers through `tenant_controls -> tenant_applicable_controls -> v_iso_control_effective_health`. |
| ADR-070 | VERIFIED_LOCAL | Post-lifecycle runtime consumers must consume the closed lifecycle contract instead of legacy physical assumptions. `controls_catalog_standards` is only a standard-control relation; clause/control text comes from `controls_catalog.clause`. Diagnostic optional legacy columns are explicit nullable projections when absent, not invented data. `refresh_kpi_health_snapshots(uuid)`, `control_health_scores`, and KPI-HLT are not reintroduced; status-change consumers read canonical `v_iso_control_effective_health`. Health ISO missing read models are materialized as real tenant-scoped canonical views over `v_iso_control_effective_health`, applicability, findings, evidences, action plans and audit logs; empty tenants return empty result sets and no synthetic score. Applicability failure metadata is recorded only after transaction rollback. Feature-disabled risk/AI capabilities are contractual states, not 500s or RBAC expansion triggers. Runner `--preflight` in `pending` state validates only safe pre-apply conditions and defers target-view postconditions until `--apply` or `already_applied`. AI Compliance `/engine-health` reports feature/engine/db state, but all other IA Compliance endpoints continue to require auth, tenant isolation, RBAC `ai.view`, and commercial capability. Configuracion process degradation is limited to known optional process contracts and must not mask arbitrary SQL drift. |
| ADR-073 | VERIFIED_LOCAL | DGX/LiteLLM is consumed through the existing `openai_compatible` AI Engine provider, not a new provider or second orchestrator. For OpenAI-compatible gateways, `call_llm_json()` preserves JSON mode, parses only `message.content`, ignores `reasoning_content` for application output, defaults `reasoning_effort` to `low`, enforces effective `max_tokens >= 2000`, and does not send Ollama-only generation options such as `num_predict` or `num_ctx`. Narrative generation remains evidence-bound: sparse authorized contexts must degrade to explicit insufficient evidence and human review. Contexts with operational evidence require a generic claim-grounding contract (`grounding_claims`/`claims` with authorized `source_refs`) before being treated as grounded; absence of valid claim refs degrades confidence and requires human review. Guided endpoints remain deterministic plus enrichment, preserve textual confidence semantics, and must not become LLM-only flows. |
| ADR-074 | VERIFIED_LOCAL | AI Engine context building must consume the canonical `ai_core` views as published instead of reintroducing legacy DB compatibility. `v_tenant_health_context`/`v_kpi_context` are metric-snapshot contexts, `v_control_context` is control identity plus `implementation_status`, and `v_finding_context` is the narrow finding projection. Python consumers may derive presentation/context buckets from canonical fields, but must not add SQL aliases, compatibility views, old aggregate columns or null-to-zero evidence/finding/action counts. Tenant scope remains mandatory and no cross-tenant fallback is allowed. |
| ADR-075 | VERIFIED_LOCAL | AI Core runtime access to canonical context views is granted through an explicit allowlist on the NOLOGIN role `tcdx_backend_runtime`. The LOGIN role `tcdx_backend_app` inherits access through its existing membership and must not receive direct grants for these views. The unexpected `ai_core` SELECT gate must recognize the previously versioned External Lookup runtime contract from `20260910_tcdx_saasv2_fresh_runtime_contract_closeout` (`external_lookup_extra_charges`, `external_lookup_logs`, `external_lookup_quota_audit`, `external_lookup_quotas`, `v_external_lookup_usage_monthly`) plus the four new context views, and must fail closed for any other `ai_core` relation. `ai_reader` remains without broad `ai_core` access. Runtime grants must be forward-only, idempotent and least-privilege: exact `SELECT` on `v_tenant_health_context`, `v_control_context`, `v_finding_context` and `v_kpi_context`; no DML, no `GRANT ALL`, no `ON ALL TABLES`, no ownership changes and no legacy compatibility objects. |
| ADR-076 | VERIFIED_LOCAL | Guided AI canonical knowledge must consume KB v2/reference knowledge directly and must not recreate the removed AI Core expert model. Runtime reads for problem knowledge, domain knowledge, finding scenarios, supervised feedback and external lookup use `public.knowledge_*`, `public.iso_evidence_expectations`, tenant-scoped evidence requirements and optional `recommendation_decision_ledger` only. Missing knowledge, questions, rules, feedback or scenarios are insufficiency/provenance states, not zero/compliance/closure. Audit questions and rules may guide human validation but do not become automatic closure criteria. Ledger decisions are not effectiveness evidence: `accepted`, `modified` and `executed` do not imply usefulness; effectiveness belongs to `recommendation_effectiveness_evaluations`. Canonical domain matches are compatibility signals and must not fabricate high relevance/criticality from match counts. External lookup treats `knowledge_sources` as trusted only with explicit trust metadata and allowed domains; `active`/`use_in_system`/presence in knowledge are insufficient by themselves. |
| ADR-077 | VERIFIED_LOCAL | AI Guided canonical knowledge runtime ACL is least-privilege over the existing backend runtime role chain: grant only seven missing child-table `SELECT` privileges to NOLOGIN `tcdx_backend_runtime`; `tcdx_backend_app` must inherit via membership and `INHERIT`; `ai_reader` receives no new access; DML, direct login grants, schema-wide grants, ownership changes and legacy `ai_core` recreation remain forbidden. The runner keeps an unexpected `public` SELECT fail-closed gate for the fresh deploy contract, but the allowlist is derived statement-by-statement from versioned fresh strategy SQL plus this migration and is tested against single grants, comma lists, mixed privileges, schema-qualified/unqualified names, `TABLE`/`VIEW`, multiline lists and schema-grant exclusion. Pre-apply is convergent/idempotent for target SELECT grants already present, while unsafe privileges, checksum mismatch and unexpected public runtime SELECT fail closed. |
| ADR-078 | VERIFIED_LOCAL | AI Guided product-ready runtime for `POST /api/ai/suggest/finding-analysis` and `POST /api/ai/suggest/action-plan` is canonical-only: these two endpoints must not execute `_safe_legacy_call`, `ai-engine/knowledge_client.py` or backend internal-search legacy routes. The contract is canonical context + canonical KB + deterministic facts + optional DGX/LiteLLM enrichment through `app.services.llm_client` + structural validation + deterministic fallback. Runtime problem type coverage is held in existing `public.knowledge_*` tables as tenant-neutral `derived_summary` operational guidance, not in a recreated `ai_core.problem_types` model. Missing domain/standard mapping is `unknown`, not false. Closed findings are historical and cannot add active finding/nonconformity signal. LLM output is recommendation-only, cannot invent evidence/compliance/Health/KPI/effectiveness/responsibles/dates, cannot auto-close, and any invalid/unavailable model response falls back with explicit trace. |
| ADR-079 | VERIFIED_LOCAL | ISO9001 data/evidence systemic review cannot replace governed reference content by inference: the active repository authority has `ISO9001:2015` approved/product-projected with 16 controls and `ISO9001:2026_FDIS` transition-only with 8 controls. A ~50-control ISO9001:2015 universe requires a governed source/manifest update and human approval, not model invention. Runtime projections must use `tenant_controls + tenant_applicable_controls` as the user-visible control universe; Health snapshots are optional measurements and cannot hide controls or override freshly saved operational status. Legacy control update compatibility must prefer `tenant_controls.id` and publish GRC post-mutation orchestration; local-disk evidence upload must clean up physical files on failed validation/transaction and remains subject to VM persistence/permission validation. |
| ADR-080 | VERIFIED_LOCAL | Canonical backend dataflow closeout adapts runtime to the current `tcdx_saasv2` schema without DDL: evidence-library manual upload is a virtual-source workflow over `document_index` with `provider='manual_upload'`, `source_id=NULL` and `integration_id=NULL`; it must not require or recreate `tenant_document_sources`/`tenant_integrations`. Manual upload file writes are atomic with DB persistence by deleting only the newly written tenant-root file on persistence failure. `PUT /api/controls/:id` is canonical-only over `tenant_controls` and must not fallback to `UPDATE controls`. Dashboard control existence is operational/applicability state; Health is optional enrichment and missing Health remains no-data. Tenant joins from operational suggestion consumers back to `tenant_controls` must include tenant scope. |

Una ADR `VERIFIED` sólo se revisa por evidencia objetiva, regresión, cambio aprobado de requisito, incompatibilidad demostrada o riesgo de seguridad.

## DB-N05 versioned QA reference closeout — 2026-09-08

Status: `DB_N05_READY_FOR_REVIEW`. Local gates only; production creation remains prohibited until integral Human Review DB-N01 -> DB-N05.

| Version | Controls | Evidence expectations | Publication | Certifiable | Status |
| --- | ---: | ---: | --- | --- | --- |
| ISO9001:2015 | 16 | 13 | published | true | approved_for_loader |
| ISO27001:2022 | 21 | 19 | published | true | approved_for_loader |
| ISO42001:2023 | 16 | 14 | published | true | approved_for_loader |
| ISO9001:2026_FDIS | 8 | 8 | transition_prep | false | transition_only |

The reviewed authority is `iso_controls` plus same-version `iso_evidence_expectations` from this export. Coverage means faithful materialization of the approved product reference snapshot, whose source_policy is copyright_safe_summary; it does not assert reproduction of the full normative publications.

ISO9001:2026_FDIS is transition preparation only. It has no `product_standard_code`, no row in the selectable `standards` compatibility catalog and no projection into `controls_catalog`. It does not replace ISO9001:2015 and must not be sold or represented as final/certifiable ISO9001:2026. Absence of a final 2026 catalog is not a blocker for this review gate. Historical ISO27701/27017/27018 compatibility is preserved and outside current blockers.

The sole orchestrator remains `scripts/normalization/load-production-reference-catalogs.js`. Manifest v2 stores raw standard_code/version_code, explicit product compatibility code, publication/certification status, loader_status, controls/evidence/metadata paths, exact counts and SHA-256 for each file. Three entries are approved_for_loader; FDIS is transition_only. Every file and same-version evidence mapping is validated before any reference write, and before the main orchestrator connects. Pending/unapproved states, checksum or count mismatches, missing titles, duplicate controls/evidence and cross-version references fail closed.

Fresh baseline adds `iso_standards`, `iso_standard_versions`, `iso_controls`, `iso_evidence_expectations`, with natural-key uniqueness and composite same-version FKs. New surrogate IDs come from PostgreSQL, not QA. Runtime backend/platform/support have SELECT only on these four tables; ai_reader has no access; migration admin loads references. No schema-wide grants or new functions were added.

All four sources load into versioned tables. Only the three approved published versions project to existing product standards/controls/mappings. Existing 30 KB-derived ISO27001 codes and their compatibility behavior remain; metadata marks them compatibility_only. They are not counted as approved versioned controls and no semantic equivalence to QA ISMS codes is invented. Fresh controls_catalog/mappings=83: 53 approved projected controls + 30 preserved KB codes. QA controls_catalog's 118 exported rows and 118 mappings are not imported.

All requested local gates PASS, including 63 runtime schema checks, real isolated fresh double-load/idempotence, effective reference ACL and ten established DB-N01..DB-N04 focal regressions. Source SHA-256: `5ebe8c04225bbe19a0f5b4a56b648af8fc43019f0195b8854384a4bbbf9c3ab0`. Handoff: `docs/handoffs/DB-N05_FRESH_PRODUCTION_DATABASE.md`. Next: STOP for integral Human Review; no tcdx_saasv2 creation.

## DB-N05 historical local evidence — 2026-09-07 (scope superseded)



Status: `DB_N05_PARTIAL`; remaining blocker `MISSING_PRODUCTION_REFERENCE_SOURCE`.
Branch `codex/db-n01-control-identity-normalization`; HEAD `11003dd92385dcca1caf5365fa437be3aee1f648`; dirty DB-N01..DB-N04 work preserved; no staging/commit/push/merge/deploy or external DB writes.

Current evidence supersedes previous missing-pgvector/static-only reports:

- PostgreSQL 16.15 with local pgvector/pgvector:pg16 image ID sha256:ccc6e83d6e35e931dc7c5def2022729d5a6c370318d099181995567ff1fb4d6b. Disposable container, localhost-only random published port, tmpfs data, container/volumes removed. Helper supports auto/local/docker and retains mandatory vector.
- ACTIVE_MISSING=0, 60 focused checks; regulatory table/constraint/index parity PASS for all 15 scoped F6.11-A/B tables. SQL baseline executed twice successfully. Bounded coverage is not a whole-product runtime certification.
- Effective privileges PASS using real SET ROLE: migration admin bootstrap/rerun, backend DML, backend admin-only DML denied, support SELECT/write denial, platform catalog update/tenant DML denial, ai_reader no direct SELECT, no super/BYPASSRLS/CREATE privileges for runtime roles, and `R00 FUNCTION_EXECUTE_ALLOWLIST PASS`. Broad `EXECUTE ON ALL FUNCTIONS` was removed; explicit job/output grants follow existing asyncJob and official calculation consumers; app_roles and plan_version_capabilities are canonical.
- Real catalog load twice PASS; stable identities/content/counts checked. KB derived children are rebuilt by the existing loader: logical content is compared excluding generated child IDs/timestamps; the import audit records both completed runs. Counts: 4 standards, 30 ISO codes/mappings (26 leaf controls + 4 families), 62 permissions, 45 commercial capabilities, 135 plan/capability rows, 18 modules, 53 official formulas/versions, 20 official/semantic source contracts, 22 indicators/versions/bindings, 5 KB sources, 1000 items, 1000 each evidence expectations/questions/gaps/actions/rules/hints, 6000 mappings.
- Fresh gates PASS: BOOTSTRAP_RERUN, CATALOG_LOAD_1, CATALOG_LOAD_2, IDEMPOTENT, CATALOG_LOADER_RERUN, PGVECTOR_VALIDATION, FUNCTIONAL_VALIDATION, MULTITENANT_VALIDATION, LEGACY_ABSENCE, INTEGRITY_VALIDATION, BACKEND_STARTUP, FRESH_DB_FROM_ZERO. OBJECT_COUNTS=117|193|253. Backend startup uses a login member of tcdx_backend_runtime, not postgres.
- Catalog completeness FAIL: the canonical SoA registry declares ISO27001/ISO27701/ISO27017/ISO27018, while the inspected versioned KB/source inventory has no complete production source for ISO27701/ISO27017/ISO27018. ISO27001's 30 derived codes are not a complete reviewed control manifest; ISO9001 has KB reference coverage but no loaded complete clause/control catalog. No legal/control content invented and no product promises silently disabled.
- Ten requested DB-N01..DB-N04 regression commands PASS. Full backend suite/CI not run. No backend runtime edits in this continuation.

Next: supply or identify governed complete reference sources/manifests for the declared product families, wire them through the same loader and close the failing catalog gate. Review application role mappings for any newly registered commercial permission identities; the loader does not silently grant those permissions. No real production creation and no DB-N06. See artifacts/db-n05/VALIDATION_RESULTS.md and REFERENCE_CATALOG_COVERAGE.md.

## DB integral human review — 2026-09-09

| ADR | Estado | Decisión |
|---|---|---|
| ADR-057 | VERIFIED_LOCAL | DB integral DB-N01..DB-N05 retires fresh-baseline/runtime legacy authority: new control identity is `tenant_controls.id` with `controls_catalog.id`; `controls`, `control_health_scores`, `refresh_control_health_scores_v2_1`, `v_latest_health_kpi_snapshots`, `KPI-HLT-*`, `legacy_control_id`, `controls_id_legacy` and `evidences.control_id` are not fresh runtime authorities. |
| ADR-058 | VERIFIED_LOCAL | Per-control Health authority is `public.v_iso_control_effective_health` over official `metric_snapshots` only when `metric_code='F5_5_CONTROL_EFFECTIVENESS'` and explicit `tenant_control_id` exists in snapshot payload/metadata. Global `F5_5_GRC_HEALTH` is a tenant-level score and must not populate control-level Health. Missing control snapshots preserve `NULL` score and explicit not-measured state. |
| ADR-059 | VERIFIED_LOCAL | Formula numeric golden validation must compare against active formula output precision/version. `F5_5_SEVERITY_INDEX` v1 remains `(1Nb+2Nm+3Na+4Nc)/(4N)*100`; the raw fixture input `low=2, medium=2, high=1, critical=1` yields raw `54.1667` and official precision-2 output `54.17`. Formula expressions and source fixture authority were not changed for this mismatch. |
| ADR-060 | VERIFIED_LOCAL | Productive formula runtime entrypoint is `officialCalculationOrchestrator.service.js -> recalculateOfficialAnalytics`; publication to `metric_snapshots` is a separate official indicator governance phase through `indicatorGovernance.calculateIndicator/createSnapshot/publishSnapshot`. Tests must not replace this with manual inserts when claiming runtime E2E. |
| ADR-061 | VERIFIED_LOCAL | Risk formulas resolve from canonical `risks` plus `risk_control_relations` and tenant control identity in the fresh baseline. Legacy or fallback sources are not authoritative when canonical rows exist. |
| ADR-062 | VERIFIED_LOCAL | TCDX SaaSv2 runtime contract V2 treats `tenant_nonconformities.control_id` and `evidences.control_id/catalog_control_id` as catalog identity (`controls_catalog.id`), while `tenant_control_id` remains operational identity (`tenant_controls.id`). Runtime inserts without `title` are valid; database triggers populate compatibility `title` from real description/control/file metadata and block divergence when a tenant control implies a different catalog control. Evidence AI and standard lifecycle runtime tables are additive tenant-aware DB contracts with explicit grants to `tcdx_backend_runtime`, not production readiness evidence. |
| ADR-063 | VERIFIED_LOCAL | Published source contract checksums are immutable contract identifiers. `risk_register_controls@7` remains `b335b8d8f1c516153b45c53821930fb5c1251fb3bd1b4e112f1c5b3bec00fa80`; no v8 is created without semantic contract drift. Phase 5-C3 must compare the persisted `official_formula_source_contracts.checksum` against the same published code checksum and, on mismatch, expose `db_checksum`, `code_checksum` and differing persisted fields instead of reporting an opaque mismatch. Deep stable JSON may be used for diagnostics/tests, but it is not a retroactive replacement for the published v7 checksum. |
| ADR-064 | VERIFIED_LOCAL | Application deploy for DB-N05 fresh production must explicitly select a database strategy. `fresh-baseline` is the only valid strategy for `tcdx_saasv2`; it must not replay historical Phase 3/4/5/F6/RBAC/commercial/normalization/hotfix runners and must not run baseline/seed/reference loader during recurrent deploy. `historical-upgrade` remains available only for non-`tcdx_saasv2` existing databases. Deploy must fail closed after resolving `MIGRATION_DATABASE_URL` and backend runtime `.env` identity without printing secrets. |
| ADR-065 | VERIFIED_LOCAL | TCDX SaaSv2 fresh runtime closeout is a forward-only physical runtime contract patch, not a product catalog bootstrap. It may add active backend-required columns, relations, views, functions and explicit runtime grants, but must not replay historical migrations, must not use `DROP VIEW ... CASCADE`, must not seed invented `saas_price_catalog` prices or default external lookup quota rows, and must fail closed on unlisted view dependencies. RBAC helpers map legacy aliases to canonical role keys only for permission resolution, without rewriting users or granting tenant roles platform permissions. |
| ADR-066 | VERIFIED_LOCAL | Final release RBAC pre-deploy closes residual executable role authority: local role lists/helpers may remain only as consumers of `roleCompatibility.service.js`, frontend mirror compatibility, presentation-only labels, or explicit non-user business semantics such as `internal_ai`/workflow assignment. Any executable authorization, scope, mutation/read gate, platform family decision, dealer family decision or role assignment validation must delegate to the canonical role resolver, RBAC permission checks, tenant scope, entitlement/capability, or dealer assignment. |
| ADR-067 | VERIFIED_LOCAL | TCDX commercial runtime integral closeout separates active runtime contracts by behavior: `tenant_standard_audit`, `tenant_company_profiles`, `tenant_applicable_controls`, `tenant_document_object_links`, `findings.due_date` and `tenant_subscription_addons.tenant_id` are required physical fresh-runtime contracts covered by forward-only migration `20260910_tcdx_commercial_runtime_integral_closeout`; runtime no longer requires SQL function `normalize_status_for_audits(text)` because audit/report routes use inline SQL status normalization; `search_history` is optional and must degrade safely to empty history/no-op tracking when the relation is absent. The commercial route test must not depend on binding a local TCP port. |
| ADR-068 | VERIFIED_LOCAL | Fresh baseline runtime dependency systemic closeout treats active executable backend SQL as the bounded contract for fresh production, but does not create schema from every textual regex hit. `tenant_controls.notes` is not a fresh runtime contract and must not be added; `initialize-controls` writes `tenant_standard_id`, leaves `score` NULL/`sin_datos`, avoids arbitrary operation assignment when several active operations exist and remains idempotent by `(tenant_id, control_id)`. Per-control Health remains `v_iso_control_effective_health` over `metric_snapshots.metric_code='F5_5_CONTROL_EFFECTIVENESS'`; `control_health_scores` and KPI-HLT are not reintroduced. `report_types`/`report_access_rules` seed only global product catalog/access rows, not tenants or users. ISO Express and Operational Suggestions reuse historical authoritative shapes where present, add same-tenant integrity for child rows, relax legacy `suggestion_key/source_type/recommendation` NOT NULL constraints in favor of runtime `dedupe_key/source_module/description`, and grant only explicit privileges to `tcdx_backend_runtime`, which remains NOLOGIN. |
| ADR-071 | VERIFIED_LOCAL | GRC runtime functional repair keeps `officialCalculationOrchestrator` as the single calculation authority and treats `grcCalculationOrchestration.service.js` only as a post-mutation adapter. Clean runtime schema does not require `action_plans.description`, `priority`, `owner` or diagnostic suggestion `target_label`; consumers must use schema-aware projection/fallback or honest nulls, never add compatibility columns. Diagnostic SoA assessment writes must pass `tenant_controls.id` and active tenant standard identity. Versioned tenant standard codes such as `ISO_27001_2022` are normalized for ISO Express eligibility without inventing aliases, and compact `ISO27001` style is a display concern only. Per-control Health UI/API must preserve `sin_datos`/`unmeasured` and cannot derive `saludable` from local `tenant_controls.score` when canonical Health is absent. IA for NC is assistive through existing AI Compliance capability/RBAC, not a compliance authority. |
| ADR-072 | VERIFIED_LOCAL | Final directed GRC correction tightens ADR-071: `actionPlanPersistence` may preserve only explicitly governed optional absent fields in `metadata`; unknown or misspelled fields fail closed with a contract error instead of being ignored. ISO Express standard identity must be resolved by generic syntactic normalization plus real `iso_standard_versions` records and tenant standard membership, with no norm-number whitelist or SQL `LIKE` aliases. Control workbench Health has an inseparable canonical pair: absent/non-finite score or `sin_datos` status yields `effective_health_status='sin_datos'` and `effective_health_score=NULL`; measured statuses require an official score from `v_iso_control_effective_health`, and summary/filter/badges use the same normalized state. |
| ADR-073 | VERIFIED_LOCAL | AI Guided product-ready final review closes the remaining four local FAILs without changing canonical authority: the product-ready catalog runner must expose `--checksum`, read-only `--preflight` and write-only `--apply`; DGX/LiteLLM enrichment is narrative-only and factual fail-closed; `canonical_knowledge_match_count` is unique matched `knowledge_items` with separate real child counts; `finding-analysis` and `action-plan` use `public_contract=ai_guided_product_ready_v1` and cannot execute or serialize legacy public response fields. |
